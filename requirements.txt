# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
google-genai>=1.0.0
langgraph>=0.2.0

# Database (uncomment as needed)
# sqlalchemy>=2.0.0
# alembic>=1.14.0
# psycopg2-binary>=2.9.0

# Vector Store (uncomment as needed)
# chromadb>=0.5.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# Triage dataset catalogs (scripts/red_flag_catalog.py, evidence_catalog.yaml)
pyyaml>=6.0

# NOTE: scripts/translate_patient_doctor_train.py additionally needs torch +
# transformers + sentencepiece. Those live in requirements-translate.txt and are
# deliberately not installed here -- torch alone is ~2.5 GB and CI never runs
# that script.
