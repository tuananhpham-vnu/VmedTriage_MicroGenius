# Thu muc `src/tool/cds`

Thu muc nay khai bao descriptor cho CDS Hooks. CDS Hooks la co che lay clinical decision support cards cho clinician-facing workflow.

## File

| File | Lam gi |
|---|---|
| `__init__.py` | Danh dau package `src.tool.cds`. |
| `cds_hooks.py` | `build_descriptor()` tao descriptor cho tool goi CDS Hooks triage advice. |

