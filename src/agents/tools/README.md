# Thu muc `src/agents/tools`

Thu muc nay chua tool mau cho agent. Trong luong VMedTriage hien tai, pipeline chinh dung `src/tool/catalog` va `ToolOrchestrator`, khong phu thuoc truc tiep vao cac tool mau nay.

## File

| File | Lam gi |
|---|---|
| `__init__.py` | Danh dau package `src.agents.tools`. |
| `example_tool.py` | Vi du cach khai bao tool LangChain: `search_knowledge()` tra ve ket qua gia lap, `calculate()` tinh toan bieu thuc so hoc an toan bang AST. |

